describe('[data-compiler="layout"]', () => {
  let component

  beforeEach(() => {
    const container = loadPreviewScenario('layout', 'default')
    component = container.querySelector('.layout')
  })

  it('works', () => {
    // TODO: implement spec
  })
})
