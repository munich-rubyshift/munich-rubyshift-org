<% content_for :title, "Event: #{@events_event.slug}" %>

<%= render @events_event %>

<% if defined?(Avo) %>
  <p>
    <%= link_to "✏️", avo.resources_events_event_path(@events_event), "up-follow": false %>
  </p>
<% end %>

<%= link_to "Back to all events", events_events_path %>
