<% content_for :title, "Additional resource: #{@talks_additional_resource.slug}" %>

<%= render @talks_additional_resource %>

<% if defined?(Avo) %>
  <p>
    <%= link_to "✏️", avo.resources_talks_additional_resource_path(@talks_additional_resource), "up-follow": false %>
  </p>
<% end %>

<%= link_to "Back to all additional resources", talks_additional_resources_path %>
