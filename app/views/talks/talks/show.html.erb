<% content_for :title, "Talk: #{@talks_talk.slug}" %>

<%= render @talks_talk %>

<% if defined?(Avo) %>
  <p>
    <%= link_to "✏️", avo.resources_talks_talk_path(@talks_talk), "up-follow": false %>
  </p>
<% end %>

<%= link_to "Back to all talks", talks_talks_path %>
