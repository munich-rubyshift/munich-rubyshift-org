<% content_for :title, "Venue: #{@venues_venue.slug}" %>

<%= render @venues_venue %>

<% if Rails.env.development? %>
  <p>
    <%= link_to "✏️", avo.resources_venues_venue_path(@venues_venue), "up-follow": false %>
  </p>
<% end %>

<%= link_to "Back to all venues", venues_venues_path %>
