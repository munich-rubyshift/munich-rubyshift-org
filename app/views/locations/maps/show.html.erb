<% content_for :title, "Map: #{@locations_map.slug}" %>

<%= render @locations_map %>

<% if defined?(Avo) %>
  <p>
    <%= link_to "✏️", avo.resources_locations_map_path(@locations_map), "up-follow": false %>
  </p>
<% end %>

<%= link_to "Back to all maps", locations_maps_path %>
