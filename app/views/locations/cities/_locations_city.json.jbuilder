json.extract! locations_city, :id, :slug, :name, :rubyevents_slug, :state_code, :country_code, :locations_coordinates_id, :created_at, :updated_at
json.url locations_city_url(locations_city, format: :json)
