<% content_for :title, "Address: #{@locations_address.slug}" %>

<%= render @locations_address %>

<% if defined?(Avo) %>
  <p>
    <%= link_to "✏️", avo.resources_locations_address_path(@locations_address), "up-follow": false %>
  </p>
<% end %>

<%= link_to "Back to all addresses", locations_addresses_path %>
