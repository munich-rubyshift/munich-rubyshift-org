<% content_for :title, "Coordinates: #{@locations_coordinates.slug}" %>

<%= render @locations_coordinates %>

<% if defined?(Avo) %>
  <p>
    <%= link_to "✏️", avo.resources_locations_coordinates_path(@locations_coordinates), "up-follow": false %>
  </p>
<% end %>

<%= link_to "Back to all coordinates", locations_coordinates_index_path %>
